# [통합 상세 기획서] 모아 캘린더 (Moa Calendar) v2.0
**부제: 관계와 맥락을 담은 올인원 공유 자산 관리**

> 본 문서는 REQUIREMENTS_DETAIL.md의 내용을 포함하고, 미정의 항목을 모두 보완한 **단일 참조 문서**입니다.
> 화면 기획은 별도 문서 `REQUIREMENTS_VIEW.md`를 참조하세요.

---

## 1. 서비스 개요

### 1.1 기획 의도
- **명칭:** 모아 캘린더 (Moa Calendar)
- **컨셉:** 일정(Context)과 가계부(Transaction)를 하나의 캘린더에서 통합 관리. "약속이 있으면 지출이 발생한다"는 자연스러운 맥락을 앱에 구현.
- **확장성:** 1인 개인 비서 → 연인·부부·모임의 공유 가계부로 자연스럽게 확장.

### 1.2 핵심 가치
1. **Contextual (맥락 연결):** 지출을 특정 일정에 연결하여 '무엇 때문에' 썼는지 기록.
2. **Flexible (유연성):** 일정 연결형 지출 + 날짜 독립형 지출 모두 지원.
3. **Separable (명확한 구분):** 내 자산(Personal)과 공용 자산(Joint)을 색상과 태그로 명확히 분리.

---

## 2. 서비스 정책 및 계정

### 2.1 인증 및 회원가입
- **가입 방식:** 이메일(ID) + 비밀번호.
- **프로필 설정:** 닉네임, 프로필 이미지(선택), **고유 색상(8종 택 1)**.
- **초기 세팅 (Onboarding):**
    - 회원가입 직후 **'내 캘린더'**의 **이름**(예: 민수의 하루)과 **테마 색상**을 설정.
    - 가계부 시작일은 기본값 **1일**로 자동 지정 (이후 설정에서 변경 가능).

### 2.2 캘린더 = 그룹 (Calendar is a Group)
- **내 달력 (My Calendar):** 회원가입 시 자동 생성되는 1인 PERSONAL 그룹.
- **공유 달력 (Shared Calendar):** 멤버를 초대하여 함께 사용하는 SHARED 그룹.
- 유저는 여러 그룹에 소속 가능하며, 상단 드롭다운으로 전환.

### 2.3 유저 권한 및 역할
- **호스트(Host):** 그룹을 생성한 관리자. 초대/강퇴/그룹 삭제 권한.
- **게스트(Guest):** 초대를 통해 합류한 멤버. 호스트와 **동등한 쓰기/수정 권한**.
- **멤버 수:** 그룹당 최대 **10인**.

### 2.4 초대 정책 (In-App Invite)
1. 호스트가 `햄버거 메뉴 > 멤버 초대`에서 **이미 가입된 유저의 이메일(ID)**을 검색하여 초대 발송.
2. 초대받은 유저가 앱에 진입(로그인)하면 **초대 팝업** 노출.
3. **[수락]** 시 그룹에 합류, **[거절]** 시 팝업 닫힘.
- 초대 이력은 Invites 테이블에 기록 (감사 로그).
- 수락 시 Group_Members에 ACCEPTED 상태로 삽입.

---

## 3. 핵심 기능 로직

### 3.1 캘린더 & 일정 (Schedule)
- **뷰:** 달력은 항상 **1일~말일** 표시 (가계부 시작일과 무관).
- **시각화:** 유저별 고유 색상(8종)으로 누구의 일정인지 바(Bar)로 표시.
- **종일 vs 시간 지정:**
    - `is_all_day = true`: 시간 없이 날짜 상단 바(Bar)로만 표시.
    - `is_all_day = false`: 시작/종료 시간 지정, 바(Bar) + 시간 텍스트.
- **다일(Multi-day) 일정:** `start_date`와 `end_date`가 다르면 연속 바(Bar)로 렌더링.
- **성격 구분:**
    - **Personal (개인):** 나의 사적인 약속.
    - **Joint (공용):** 구성원 모두의 약속. *공유 그룹에서만 선택 가능.*
- **반복 규칙:**

| 옵션 | repeat_type 값 | 설명 |
|------|---------------|------|
| 반복 없음 | `NONE` | 기본값 |
| 매일 | `DAILY` | |
| 매주 | `WEEKLY` | 생성 시점의 요일 기준 |
| 매월 | `MONTHLY` | 생성 시점의 일자 기준 |
| 매년 | `YEARLY` | 생성 시점의 월/일 기준 |

- **데이터 사전 생성 방식:** 반복 설정 시 사용자가 **반복 종료일**을 지정하면, 시작일~종료일 범위 내의 모든 개별 레코드를 **즉시 생성**(INSERT)한다.
- 같은 반복에서 생성된 레코드는 `repeat_group_id`로 묶인다.
- 개별 레코드 수정/삭제 시 해당 건만 영향. 반복 그룹 전체 삭제도 가능.

### 3.2 가계부 (Transaction)
- **등록 방식의 이원화:**
    1. **일정 연결형:** `[Link to Schedule]`로 같은 날짜의 일정을 선택하여 연결.
    2. **날짜 독립형:** 일정 연결 없이 날짜에 귀속 (예: 공과금, 편의점).
- **자산 성격 (Asset Type):**
    - **Personal (개인):** 내 지출/수입 (기본 분홍색, 커스텀 가능).
    - **Joint (공용):** 공유 지출/수입 (기본 파란색, 커스텀 가능). *멤버 초대 수락 시 활성화.*
- **자산 출처 (Asset Source):** 현금, 카드, 통장 등 실제 결제 수단.
- **통화:** v1은 **KRW(원) 단일 통화**. 금액 표시: `#,###원` (정수).
- **반복 수입/지출:** 일정과 동일한 `repeat_type` + 사전 생성 방식. 급여, 월세, 구독료 등 정기 항목에 활용.

### 3.3 가계부 시작일 (Budget Cycle)
- **기본값:** 매월 **1일**.
- **변경:** `햄버거 메뉴 > 앱 설정 > 가계부 시작일`에서 1~28일로 변경.
- **영향:** 통계/예산 계산 기준만 변경. 달력 뷰는 항상 1일~말일.
- **공유 그룹:** 호스트만 변경 가능. 변경 시 전원에게 적용 (공용 정산 주기).

### 3.4 필터 시스템

| 필터 | 일정 표시 | 가계부 표시 | 설명 |
|------|----------|------------|------|
| **All** | 모든 멤버의 일정 | 모든 멤버의 Personal + Joint | 전체 보기 (기본값) |
| **Me** | 내가 작성한 일정만 | 내 Personal만 | 용돈, 비상금 등 순수 개인 내역 |
| **Joint** | Joint 일정만 | Joint 가계부만 | 공용 내역만 필터링 (공유 그룹에서만 노출) |

- **싱글 모드(PERSONAL 그룹):** `All`과 `Me` 동일. `Joint` 필터 숨김.
- **공유 모드(SHARED 그룹):** 세 필터 모두 노출.

### 3.5 인터랙션 (2-Tap)
- **구현 방식:** 시간 기반 더블탭이 **아닌 상태 기반 재터치**.
- **로직:**
    1. 선택되지 않은 날짜를 터치 → **1st Tap:** 해당 날짜 선택 (포커스), 하단 리스트 표시.
    2. 이미 선택된(포커스 상태인) 날짜를 다시 터치 → **2nd Tap:** 입력 모달 호출.
- **시간 제한 없음.** 다른 날짜를 터치하면 포커스 이동 → 이전 날짜의 상태 리셋.
- **하단 [+] 버튼:** 리스트 영역 내에서 언제든 입력 모달 호출 가능.

---

## 4. 멤버 관리 정책

### 4.1 강퇴 (호스트 → 게스트)
- **권한:** 호스트만 가능.
- **대상 데이터 처리:**

| 데이터 유형 | 처리 |
|------------|------|
| 강퇴 대상이 작성한 **Joint 일정** | **영구 삭제** |
| 강퇴 대상이 작성한 **Joint 수입/지출** | **영구 삭제** |
| 강퇴 대상이 작성한 **Personal 일정** | 그룹에서 제거, **본인 개인 캘린더로 이관** |
| 강퇴 대상이 작성한 **Personal 수입/지출** | 그룹에서 제거, **본인 개인 캘린더로 이관** |

- 삭제 기준: `asset_type = 'JOINT'`로 설정된 데이터만 삭제.
- 확인 팝업: "[닉네임]님을 내보내시겠습니까? 해당 멤버의 공용 데이터가 삭제됩니다."

### 4.2 자발적 탈퇴 (게스트 스스로)
- **진입:** `햄버거 메뉴 > 캘린더 관리 > [캘린더 나가기]`.
- **데이터 처리:**

| 데이터 유형 | 처리 |
|------------|------|
| 본인이 작성한 **Joint 데이터** | 그룹에 **유지** (공용 자산) |
| 본인이 작성한 **Personal 데이터** | **본인 개인 캘린더로 이관** |

- 확인 팝업: "캘린더를 나가면 공용 데이터에 접근할 수 없습니다. 나가시겠습니까?"
- 탈퇴 후 메인 화면은 본인 개인 캘린더로 전환.

### 4.3 그룹 삭제/해산
- **권한:** 호스트만 가능. `햄버거 메뉴 > 캘린더 관리 > [캘린더 삭제]`.
- **처리:**
    - 모든 Joint 데이터 영구 삭제.
    - 각 멤버의 Personal 데이터는 개인 캘린더로 이관.
    - 모든 멤버의 메인 화면이 각자의 개인 캘린더로 전환.
- 확인 팝업: "이 캘린더의 모든 공용 데이터가 삭제됩니다. 멤버 N명에게 알림이 전송됩니다."

### 4.4 일정 삭제 시 연결 처리
- 일정 삭제 시, 연결된 Transaction의 `schedule_id`를 **`NULL`로 변경** (연결 해제).
- Transaction 자체는 삭제되지 않음 → 날짜 독립형 가계부로 전환.
- 삭제 확인 시 "연결된 가계부 N건의 연결이 해제됩니다" 안내 표시.

---

## 5. 자산(Asset Source) 관리

### 5.1 자산 등록
- **진입:** 하단 `[자산]` 탭 > **[+ 자산 추가]** 버튼.
- **입력 필드:**
    - **이름:** 자유 입력 (예: "국민은행 급여통장")
    - **유형:** 현금 / 은행(통장) / 카드 중 택 1
    - **성격:** Personal / Joint
    - **초기 잔액:** 숫자 입력 (기본 0원)
- **공유 범위:** `Joint` 자산은 그룹 내 모든 ACCEPTED 멤버가 거래 시 선택 가능. `Personal` 자산은 본인만 사용.

### 5.2 잔액 계산
- `현재 잔액 = 초기 잔액 + 해당 자산의 수입 합계 - 해당 자산의 지출 합계`

---

## 6. 예산 관리

### 6.1 예산 설정
- **진입:** 하단 `[통계]` 탭 > 상단 **[예산 설정]** 버튼.
- **설정 단위:**
    - **전체 예산:** 이번 사이클(가계부 시작일 기준)의 총 지출 한도.
    - **카테고리별 예산 (선택):** 식비 30만 원, 교통 10만 원 등.
- **구분:** `Personal 예산` / `Joint 예산` / `전체 예산` 중 택 1.
- **표시:** 통계 탭에서 예산 대비 사용률을 프로그레스 바로 시각화.
- **알림:** v1에서는 알림 없이 시각적 표시만 제공.

---

## 7. 기본 카테고리 목록

회원가입 시 시스템이 자동으로 생성하는 기본 카테고리. 이모지는 카테고리에 **고정 매핑** (사용자가 직접 선택하지 않음).

### 7.1 지출 (EXPENSE) — 12종

| 이모지 | 이름 | 비고 |
|--------|------|------|
| 🍚 | 식비 | |
| ☕ | 카페/간식 | |
| 🚌 | 교통 | |
| 🏠 | 주거/통신 | 월세, 관리비, 통신비 |
| 👕 | 쇼핑 | |
| 🎁 | 선물 | |
| 💊 | 의료/건강 | |
| 📚 | 교육 | |
| 🎬 | 문화/여가 | |
| 🐶 | 반려동물 | |
| ✂️ | 미용 | |
| 📦 | 기타 | |

### 7.2 수입 (INCOME) — 6종

| 이모지 | 이름 | 비고 |
|--------|------|------|
| 💰 | 급여 | |
| 💵 | 부수입 | 프리랜서, 알바 |
| 🎊 | 용돈/선물 | |
| 📈 | 투자수익 | |
| 💸 | 환급 | |
| 📦 | 기타 | |

### 7.3 일정 (SCHEDULE) — 6종

| 이모지 | 이름 |
|--------|------|
| 📅 | 약속 |
| 🎂 | 기념일 |
| 💼 | 업무 |
| 🏥 | 병원 |
| ✈️ | 여행 |
| 📦 | 기타 |

### 7.4 카테고리 관리 규칙
- `설정 > 카테고리 관리`에서 **추가/수정/삭제/순서 변경** 가능.
- 시스템 기본 카테고리(`is_system = true`)는 이름 수정만 가능, 삭제 불가.

---

## 8. 통합 유저 시나리오

### Phase 1. 싱글 모드: 민수(Host)의 시작

**상황:** 30대 직장인 민수가 가입하여 개인 자산 관리를 시작함.

1. **가입 및 캘린더 생성:**
    - 민수는 회원가입(이메일, 비밀번호, 닉네임 "민수")을 완료한다.
    - 고유 색상으로 **파란색(#5B9FFF)**을 선택한다.
    - "캘린더를 설정해주세요" 화면에서 이름 **"민수의 하루"**를 입력하고 완료한다.
    - [System] PERSONAL 그룹 생성, 기본 카테고리 24종 생성, budget_start_day = 1.
2. **메인 진입:**
    - 1일~말일 달력이 보인다. 필터는 `[All]` `[Me]`만 표시 (Joint 필터 숨김).
3. **설정 변경 (기준일):**
    - 햄버거 메뉴(≡) > 앱 설정 > 가계부 시작일 → **25일**로 변경.
4. **일정 등록:**
    - 14일 날짜를 **따닥(2 Tap)** 누른다.
    - `[Schedule]` 탭에서 "치과 예약", 시간 19:00, 카테고리 🏥병원을 입력하고 저장.
5. **지출 등록 (일정 연결):**
    - 하단 리스트의 **[+]** 버튼을 누른다.
    - `[Expense]` 탭에서 30,000원, 카테고리 💊의료/건강, 자산 "내 신용카드A" 선택.
    - Link to Schedule에서 "치과 예약"을 선택하여 연결 후 저장.
6. **조회 및 수정:**
    - 며칠 뒤 14일을 **한 번(1 Tap)** 누른다. 하단에 일정과 지출 내역이 표시된다.
    - 지출 항목을 터치하여 수정 모달을 열고 금액을 정정한다.

### Phase 2. 확장 모드: 지은(Guest) 초대 및 합류

**상황:** 민수가 아내 지은과 캘린더를 공유하기로 함.

1. **게스트 가입 (선행):**
    - 지은은 앱을 설치하고 회원가입 (닉네임 "지은", 색상 **주황(#FF9800)**).
    - 자신의 캘린더 "지은 달력"을 설정하고 메인에 진입.
2. **초대 발송 (Host):**
    - 민수: 햄버거 메뉴 > 멤버 초대 > `jieun@email.com` 검색 > [초대 보내기].
    - [System] Invites 레코드 생성 (status=PENDING).
3. **초대 수락 (Guest):**
    - 지은이 앱 재진입 시 "민수님이 '민수의 하루' 캘린더에 초대했습니다." 팝업.
    - [수락] → [System] Invites.status=ACCEPTED, Group_Members 생성 (role=GUEST, status=ACCEPTED).
4. **화면 전환:**
    - 지은의 캘린더가 '지은 달력'에서 **'민수의 하루'**로 전환.
    - 필터 바: `[All]` `[Me]` `[Joint]` 세 필터 모두 표시.
    - 민수의 일정 바(파란색)와 가계부 내역이 보임.
    - 입력 모달에서 `[Joint]` 버튼 활성화.

### Phase 3. 협업 모드: 공용 자산 관리

**상황:** 지은(Guest)이 공용 생활비로 장을 봄.

1. **공용 지출 등록:**
    - 지은: 20일 더블 탭 → `[Expense]` 탭.
    - 금액 100,000원, Type을 **[Joint]**로 변경 (색상이 파란색 테마로 전환).
    - 자산: 민수가 등록해둔 `공용 생활비통장` 선택 → 저장.
2. **확인 (Host):**
    - 민수의 달력 20일에 "-100,000원" 표시.
    - 통계 탭에서 이번 사이클(25일 기준) 예산 차감 확인.
3. **필터 활용:**
    - 민수가 `[Me]` 필터 선택 → 용돈, 비상금 등 순수 개인 내역만 표시.
    - `[Joint]` 필터 선택 → 공용 생활비 내역만 필터링.

### Phase 4. 고급 관리: 자산과 커스터마이징

1. **자산 탭:** 민수가 하단 `[자산]` 탭에서 생활비 통장 잔액, 신용카드 누적 사용액 확인.
2. **색상 변경:** 햄버거 메뉴 > 앱 설정 > 자산 색상 설정 > 개인 자산 색상을 분홍 → 보라색으로 변경.

---

## 9. 데이터베이스 스키마

### 9.1 전체 스키마

```
Users
├── id                  BIGINT, PK
├── email               VARCHAR, UNIQUE, NOT NULL
├── password            VARCHAR, NOT NULL
├── nickname            VARCHAR, NOT NULL
├── color_code          VARCHAR(7), NOT NULL        -- 고유 색상 HEX (예: #5B9FFF)
├── personal_asset_color VARCHAR(7)                  -- 개인 자산 색상 (기본: #E91E63)
├── profile_image_url   VARCHAR, NULLABLE
├── created_at          DATETIME
└── updated_at          DATETIME

Groups
├── id                  BIGINT, PK
├── name                VARCHAR, NOT NULL
├── type                ENUM('PERSONAL', 'SHARED')
├── host_id             BIGINT, FK → Users.id
├── joint_asset_color   VARCHAR(7)                   -- 공용 자산 색상 (기본: #2196F3)
├── budget_start_day    INT, DEFAULT 1               -- 가계부 시작일 (1~28)
├── created_at          DATETIME
└── updated_at          DATETIME

Group_Members
├── id                  BIGINT, PK
├── group_id            BIGINT, FK → Groups.id
├── user_id             BIGINT, FK → Users.id
├── role                ENUM('HOST', 'GUEST')
├── status              ENUM('INVITED', 'ACCEPTED')
├── joined_at           DATETIME, NULLABLE
├── created_at          DATETIME
└── updated_at          DATETIME
    UNIQUE(group_id, user_id)

Invites
├── id                  BIGINT, PK
├── group_id            BIGINT, FK → Groups.id
├── inviter_id          BIGINT, FK → Users.id
├── invitee_email       VARCHAR, NOT NULL
├── status              ENUM('PENDING', 'ACCEPTED', 'REJECTED')
├── created_at          DATETIME
└── updated_at          DATETIME

Schedules
├── id                  BIGINT, PK
├── group_id            BIGINT, FK → Groups.id
├── user_id             BIGINT, FK → Users.id
├── title               VARCHAR, NOT NULL
├── start_date          DATE, NOT NULL
├── end_date            DATE, NULLABLE               -- NULL이면 단일 일정
├── start_time          TIME, NULLABLE               -- NULL이면 종일(All-day)
├── end_time            TIME, NULLABLE
├── is_all_day          BOOLEAN, DEFAULT true
├── asset_type          ENUM('PERSONAL', 'JOINT')
├── category_id         BIGINT, FK → Categories.id, NULLABLE
├── memo                TEXT, NULLABLE
├── repeat_type         ENUM('NONE','DAILY','WEEKLY','MONTHLY','YEARLY'), DEFAULT 'NONE'
├── repeat_group_id     BIGINT, NULLABLE             -- 같은 반복에서 생성된 레코드 그룹
├── created_at          DATETIME
└── updated_at          DATETIME

Transactions
├── id                  BIGINT, PK
├── group_id            BIGINT, FK → Groups.id
├── user_id             BIGINT, FK → Users.id
├── schedule_id         BIGINT, FK → Schedules.id, NULLABLE
├── type                ENUM('INCOME', 'EXPENSE')
├── amount              DECIMAL(15,0), NOT NULL      -- KRW 정수 단위
├── category_id         BIGINT, FK → Categories.id
├── asset_type          ENUM('PERSONAL', 'JOINT')
├── asset_source_id     BIGINT, FK → Asset_Sources.id, NULLABLE
├── date                DATE, NOT NULL
├── memo                TEXT, NULLABLE
├── repeat_type         ENUM('NONE','DAILY','WEEKLY','MONTHLY','YEARLY'), DEFAULT 'NONE'
├── repeat_group_id     BIGINT, NULLABLE             -- 같은 반복에서 생성된 레코드 그룹
├── created_at          DATETIME
└── updated_at          DATETIME

Asset_Sources
├── id                  BIGINT, PK
├── group_id            BIGINT, FK → Groups.id
├── user_id             BIGINT, FK → Users.id
├── name                VARCHAR, NOT NULL
├── source_type         ENUM('CASH', 'BANK', 'CARD')
├── asset_type          ENUM('PERSONAL', 'JOINT')
├── initial_balance     DECIMAL(15,0), DEFAULT 0
├── display_order       INT, DEFAULT 0
├── is_active           BOOLEAN, DEFAULT true
├── created_at          DATETIME
└── updated_at          DATETIME

Categories
├── id                  BIGINT, PK
├── group_id            BIGINT, FK → Groups.id, NULLABLE  -- NULL이면 시스템 기본
├── name                VARCHAR, NOT NULL
├── emoji               VARCHAR(4), NOT NULL
├── type                ENUM('INCOME', 'EXPENSE', 'SCHEDULE')
├── display_order       INT, DEFAULT 0
├── is_system           BOOLEAN, DEFAULT false
├── created_at          DATETIME
└── updated_at          DATETIME

Budgets
├── id                  BIGINT, PK
├── group_id            BIGINT, FK → Groups.id
├── category_id         BIGINT, FK → Categories.id, NULLABLE
├── asset_type          ENUM('PERSONAL', 'JOINT', 'ALL')
├── amount              DECIMAL(15,0), NOT NULL
├── cycle_month         INT                          -- 적용 월 (예: 202602)
├── created_at          DATETIME
└── updated_at          DATETIME
```

### 9.2 인덱스

```
Schedules:     INDEX(group_id, start_date)
Transactions:  INDEX(group_id, date), INDEX(user_id, date), INDEX(schedule_id)
Group_Members: INDEX(user_id, status)
Asset_Sources: INDEX(group_id, asset_type)
Invites:       INDEX(invitee_email, status)
```

---

## 10. 비즈니스 모델 (BM)

- **AdMob 배너:** 메인 화면 최하단(Safe Area 위) 320x50 배너 고정 노출.
- **Interstitial 전면 광고:** 일정/지출 등록 10회마다 전면 광고 노출 (구독 시 제거).

---

## 11. v1 스코프 외 (향후 버전)

| 버전 | 항목 | 비고 |
|------|------|------|
| v1.5 | OAuth 소셜 로그인 (카카오/구글/애플) | |
| v1.5 | 반복 일정/거래 자동 생성 | v1은 수동 등록만 |
| v1.5 | Push 알림 (일정 리마인더, 예산 초과) | |
| v1.5 | 반복 일정 개별 수정/삭제 (Exception) | |
| v2.0 | 다크모드 | |
| v2.0 | 데이터 내보내기 (CSV/Excel) | |
| v2.0 | 다중 통화 지원 | |
| v2.0 | 오프라인 캐시 정책 | |
| v2.0 | 회원탈퇴 및 데이터 영구 삭제 | |
| v2.0 | 비밀번호 찾기/재설정 | |

---

**최종 갱신일:** 2026-02-01
